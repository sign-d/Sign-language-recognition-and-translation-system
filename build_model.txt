model training and evaluation.

data loading and pre-processing - load and split data - pandas
model construction - create neural network and set up the model for training - train data - tensorflow, scikit-learn, numpy
training the model - fit model into training data - tensorflow, scikit-learn, numpy
evaluation - assess the model on test data, validation data -tensorflow, scikit-learn, numpy
save the model